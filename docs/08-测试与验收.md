# 测试与验收

## 当前测试结果

### 后端测试

```bash
cd backend
mvn test
```

**结果：96 tests, 0 failures, 1 skipped**（1 个预置跳过为 AiBriefingLocalModelIntegrationTest）

测试覆盖以下模块：

| 测试类 | 用例数 | 覆盖范围 |
|--------|--------|----------|
| RiskScoreServiceTest | 9 | 评分计算、无漏洞资产、修复建议、重算事务 |
| RiskScoreControllerTest | 9 | 4 个端点权限与数据验证 |
| DemoScannerStrategyTest | 8 | 模拟策略的 supports、执行、IP 衍生 |
| ScanTaskStageServiceTest | 9 | 阶段状态变更、进度更新 |
| ScanTaskControllerTest | 3 | 扫描任务端点 |
| CveMatchingServiceTest | 7 | CVE 版本匹配、范围匹配 |
| AuthControllerTest | 4 | 登录/注册认证流程 |
| GlobalExceptionHandlerTest | 19 | 统一异常处理（响应格式、服务器错误、参数错误、认证错误、权限错误、资源不存在） |
| AiBriefingControllerTest | 2 | AI 简报控制器 |
| AiBriefingServiceTest | 5 | AI 简报生成 |
| PluginControllerTest | 2 | 扫描策略插件 |
| NmapScannerImplTest | 2 | Nmap 扫描器 |
| SystemConfigServiceTest | 5 | 配置服务 |
| UserServiceTest | 11 | 用户服务 |
| AiBriefingLocalModelIntegrationTest | 1（跳过） | 本地模型集成测试 |

### 前端构建

```bash
cd frontend
npm run build
```

**结果：构建成功**（TypeScript 编译通过，Vite 打包完成）

### Docker 验收

- 容器状态正常（MySQL + Redis + Backend + Frontend 四容器运行）
- 前端 Nginx 服务可访问（`curl http://localhost:3000` 返回 200）
- 后端健康检查通过（`curl http://localhost:8080/actuator/health` 返回 200）
- 首次启动时 MySQL 初始化约需 60-90 秒，后端容器会等待 MySQL 就绪后启动

验收命令：

```bash
docker-compose up -d --build
curl http://localhost:8080/actuator/health
curl http://localhost:8080/api/v1/config/demo-mode
```

```bash
docker-compose up -d --build
curl http://localhost:8080/actuator/health
curl http://localhost:8080/api/v1/config/demo-mode
```

## 验收清单

### 基础启动验收

- [ ] 后端启动：`mvn spring-boot:run` → API 可访问
- [ ] 前端启动：`npm run dev` → 页面正常渲染
- [ ] 数据库连接：后端启动无连接错误
- [ ] Docker 部署：`docker-compose up -d --build` → 三个容器正常运行

### 登录验证

- [ ] 登录页正常显示
- [ ] `admin / Admin@123456` 登录成功
- [ ] JWT Token 正确返回
- [ ] 未登录访问受保护接口返回 401
- [ ] 角色权限控制正确（Admin/USER）

### Demo Mode 验收

- [ ] 侧边栏显示"演示模式"标签
- [ ] `/api/v1/config/demo-mode` 返回 `{"demoMode": true}`
- [ ] 创建 quick 扫描任务 → 完成
- [ ] 创建 full 扫描任务 → 完成
- [ ] 扫描阶段状态正确展示（9 阶段）
- [ ] 扫描进度实时更新

### 资产管理验收

- [ ] 资产列表正常加载
- [ ] 资产详情展示端口、服务、Banner
- [ ] 资产详情展示 Web 指纹、SSL 证书
- [ ] 蜜罐检测信息展示
- [ ] 攻击面拓扑图正常渲染

### 漏洞管理验收

- [ ] 漏洞列表正常加载
- [ ] 漏洞详情展示 CVE、CVSS、EPSS
- [ ] 漏洞状态更新（待处理/已确认/已修复/误报）

### 风险评分验收

- [ ] 扫描完成后自动生成风险评分
- [ ] Dashboard 显示风险资产 Top 5
- [ ] 扫描详情页显示风险评分模块
- [ ] 风险原因和修复建议不为空
- [ ] Recalculate 幂等（连续调用不产生重复记录）
- [ ] 无漏洞资产也有风险分

### 报告导出验收

- [ ] PDF 报告可下载、内容完整
- [ ] Excel 报告可下载、7 个 Sheet 齐全
- [ ] 中文内容不乱码
- [ ] 安全声明文案正确

### 其他验收

- [ ] 威胁情报查询可用
- [ ] AI 风险简报生成
- [ ] 系统设置可配置
- [ ] 操作日志记录查看

### 运行模式验收

- [ ] `/api/v1/system/mode` 返回 `{"code": 200, "data": {"mode": "DEMO", ...}}`
- [ ] `/api/v1/system/mode` 在 `SCANNER_DEMO_MODE=false` 时返回 `mode: "REAL"`
- [ ] Demo Mode 下创建任务，`scanMode=DEMO`
- [ ] Real Mode 下创建任务，`scanMode=REAL`
- [ ] Real Mode 下未勾选授权确认创建任务失败（返回 400）
- [ ] 报告导出中包含模式声明（Demo/Real 对应的声明文本）
- [ ] 侧边栏显示当前模式标签（演示模式/真实模式）
- [ ] 设置页显示运行模式信息卡片

## 安全合规检查

- [ ] 未提交真实 Token、密钥或数据库密码
- [ ] JWT 密钥、数据库密码使用环境变量注入
- [ ] `.env` 未被提交到仓库
- [ ] 仅用于授权资产场景

## 文档链接检查

- [ ] README 中的文档导航点击有效
- [ ] docs 目录文件名称与链接一致
